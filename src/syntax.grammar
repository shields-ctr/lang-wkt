@top WKT { Geometry* }

@skip { space | Comment }

Geometry {
  Name |
  Number |
  String
}

@tokens {
  Number { $[0-9]+ }

  Name { $[a-zA-Z]+ }

  String { '"' (!["\\] | "\\" _)* '"' }

  Comment { "//" ![\n]* }

  space { $[ \t\n\r]+ }

  "(" ")"
}

@detectDelim
